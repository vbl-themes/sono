@{
	Layout = "_Layout.cshtml";
}

<h1>@Model.GetTitle()</h1>

@if(@Model.GetBool("IsPost")) {
	<nav class="meta">
		@await Html.PartialAsync("_Meta.cshtml", Model)
	</nav>
}

<article>
	@RenderBody()
</article>
